{
    "setupTimeout": "240s",
    "maxRedirects": 0,
    "scenarios": {
        "constant_request_rate": {
            "executor": "constant-arrival-rate",
            "rate": 20,
            "timeUnit": "1s", 
            "duration": "300s",
            "preAllocatedVUs": 200, 
            "maxVUs": 500
        }
    },
    "thresholds": {
        "http_req_failed": [
            "rate<0.05"
          ]
      }
}